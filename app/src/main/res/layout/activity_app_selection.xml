<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.AppSelectionActivity">

    <!-- SearchView for filtering apps -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchViewApps"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:queryHint="@string/search_apps_hint" />

    <!-- ProgressBar for loading state -->
    <ProgressBar
        android:id="@+id/progressBarLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="visible" />

    <!-- RecyclerView for displaying installed apps -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewAppList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="56dp"
        android:visibility="gone"
        tools:visibility="visible"
        tools:listitem="@layout/item_app_info" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>